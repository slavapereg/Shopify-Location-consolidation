{
  "name": "shopify-webhook-forwarder",
  "version": "1.0.0",
  "description": "Vercel function to forward Shopify webhooks to GitHub Actions",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "engines": {
    "node": "18.x"
  }
} 